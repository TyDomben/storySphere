const initialState = {
    audios: [],
    isLoading: false,
    error: null,
  };
  
  const audioReducer = (state = initialState, action) => {
    switch (action.type) {
      // Future implementation for audio-related actions
      default:
        return state;
    }
  };
  
  export default audioReducer;
  