const initialState = {
    images: [],
    isLoading: false,
    error: null,
  };
  
  const imageReducer = (state = initialState, action) => {
    switch (action.type) {
      // Future implementation for image-related actions
      default:
        return state;
    }
  };
  
  export default imageReducer;
  